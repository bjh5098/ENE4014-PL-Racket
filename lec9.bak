#lang racket
(define a 10)
(define b (+a 10))
#|comment
|#
(define cube ; cube function
  (lambda(x)
    (*xxx)))

(define pow
  (lambda(x y)
    (if (= y 0)
        1
        (* x (pow x (-y 1))))))

